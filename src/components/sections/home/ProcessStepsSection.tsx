import React from "react";
import type { ProcessStepsSection } from "@/types/home";

export function ProcessStepsSection({ heading, steps }: ProcessStepsSection) {
  // TODO: Render process steps UI
  return <section>{/* Process steps UI here */}</section>;
}
